{sys, [
       {lib_dirs, ["../apps", "../deps"]},
       {erts, [{mod_cond, derived}]},
       {rel, "ziryab", "1",
        [
         kernel,
         stdlib,
         sasl,
         meck,
         bitcask,
         repobj,
         ziryab
        ]},
       {rel, "start_clean", "",
        [
         kernel,
         stdlib
        ]},
       {boot_rel, "ziryab"},
       {incl_cond, exclude},
       {profile, embedded},
       {debug_info, keep},
       {excl_archive_filters, [".*"]}, %% Do not archive built libs
       {excl_sys_filters, ["^bin/.*", "^erts.*/bin/(dialyzer|typer)",
                           "^erts.*/(doc|info|include|lib|man|src)"]},
       {excl_app_filters, ["\.gitignore"]},
       {app, sasl,   [{incl_cond, include}]},
       {app, stdlib, [{incl_cond, include}]},
       {app, kernel, [{incl_cond, include}]},
       {app, ziryab, [{incl_cond, include}]},
       {app, meck, [{incl_cond, include}]},
       {app, bitcask, [{incl_cond, include}]},
       {app, repobj, [{incl_cond, include}]}
      ]}.

{target_dir, "ziryab"}.

{overlay_vars, "vars.config"}.

{overlay, [
           {mkdir, "log/sasl"},

           {copy, "files/erl", "\{\{erts_vsn\}\}/bin/erl"},
           {copy, "files/nodetool", "\{\{erts_vsn\}\}/bin/nodetool"},

           {template, "files/app.config", "etc/app.config"},
           {template, "files/vm.args", "etc/vm.args"},

           {copy, "files/ziryab", "bin/ziryab"},
           {copy, "files/ziryab-admin", "bin/ziryab-admin"}
          ]}.
